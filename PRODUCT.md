# MCSkinSplit

> 给需要从现成 Minecraft 皮肤中提取、修正和复用造型元素的创作者：把一张难以复用的 64×64 PNG，变成一组看得懂、改得准、可回退的头发、服装与饰品组件。

## 产品速览

- **最先面向：** 经常修改现成皮肤、搬运局部造型或整理个人素材的皮肤作者、像素美术与资源包作者。
- **使用时刻：** 用户喜欢一张皮肤里的长发、上衣、手套或饰品，想单独保存、修正或用于另一张皮肤，却不想逐像素重新描一遍。
- **使用前：** 所有内容混在一张 UV Atlas 中；同一物品可能跨身体部位和内外层，复制边界、透明度与手臂模型都容易出错。
- **使用后：** 用户得到带名称、分类、精确像素范围和来源记录的组件，还能下载完整皮肤、保存单个组件或整理完整大类。
- **首个完整版本：** 导入皮肤 → 获得初始拆分 → 检查并修正 → 保存明确结果；AI 可以帮忙，但不是完成任务的前提。

## 它是什么

MCSkinSplit 是一套本地运行的 Minecraft 皮肤语义拆分与复用工作台。它不把“头、身体、手臂”这些几何区域当成最终结果，而是让用户得到“长发主体”“黑色上衣”“左手套”“靴子”“头饰”这类真正可以理解和再次使用的组件。

它也不是普通的自动切图工具：一个组件可以跨越多个身体面和 Base/Outer 图层，同一个面里也可以有多个组件。系统保存的是组件实际拥有的像素，而不是一个粗略矩形，因此拆出的内容可以继续检查、修补、归档和组合。

产品的核心变化很简单：**把只能整张使用的皮肤文件，变成可独立管理的造型资产，同时保留原图和每次修改。**

## 为什么值得做

Minecraft 皮肤看起来只是一张小图，但它同时编码了身体位置、朝向、内外层和 Slim/Wide 手臂差异。用户想复用一件衣服或一段长发时，通常要在 Atlas 与 3D 模型之间来回判断，手工找齐散落在各个面的像素，再自行处理透明区域和覆盖冲突。

现有做法最麻烦的不是“不会画”，而是**难以确认哪些像素属于同一件东西，也难以安全地试错**。MCSkinSplit 聚焦解决这两个问题：先把像素组织成语义组件，再让每次确认形成一个新版本。结果不理想时，用户可以比较、回退或从旧版本继续，不必在唯一的 PNG 上反复覆盖。

## 用户怎样使用

1. **导入皮肤：** 选择一张 64×64 PNG，确认 Slim/Alex 或 Wide/Classic 手臂布局，并同时查看 Atlas 与 3D 外观。
2. **获得初始拆分：** 启动可选的智能识别，或直接从人工模式开始；系统把可见像素整理为头发、服装、肤色、五官和饰品等组件。
3. **检查与修正：** 选择组件，隐藏或单独查看它，在 2D/3D 中用画笔、框选、同色选择、整面选择、Unknown、合并、拆分和改分类等操作调整结果。草稿可以撤销、重做，确认后才生成正式版本。
4. **保存与复用：** 选择要保留的版本，下载完整皮肤 PNG，或把一个组件保存为 Part、把同一大类保存为 Bundle，供后续修补和组合使用。
5. **出错时继续：** 切回历史版本比较差异、从旧结果开始另一种尝试，或直接保留原识别结果；旧结果不会被原地覆盖。

## 核心组成

### 1. 语义组件，而不是几何切块

拆分结果围绕用户真正想使用的对象组织。每个组件绑定精确像素范围，能够表达跨身体部位、跨 UV 面和跨内外层的头发、衣服或饰品。

### 2. 看得见、改得准的检查工具

组件树、像素画布和 3D 角色保持联动。用户可以显隐、Solo、圈选、比较纹理与组件归属，并在提交前预览镜像或接缝扩选。系统辅助判断，最终边界由用户确认。

### 3. 可以放心试错的版本与来源

导入、识别、人工修正、部件应用、修补和混搭都会保留旧结果。逐像素来源区分原图像素、人工创作和已接受的生成内容，避免把推测内容悄悄伪装成原作。

### 4. 从一次拆分走向重复使用

确认后的组件可保存为独立 Part；完整的头发、服装或饰品集合可保存为 Bundle。再次应用或组合时，系统先展示模型、图层和像素冲突，再让用户选择处理方式。

### 5. 明确受限的隐藏内容候选

被遮挡的像素并不存在于源 PNG，无法被事实性“还原”。实验性的 Completion 只提供带证据和生成范围的候选，允许用户比较、微调、接受或拒绝；它不会自动改写皮肤，也不会自动发布资产。

## 第一版产品

### 最小完整闭环

导入一张现成皮肤 → 获得可见组件的初始分类 → 修正边界与分类 → 选择已确认版本 → 下载皮肤或保存可复用组件。

### 第一版包含

- 64×64 RGBA 皮肤导入、Slim/Wide 布局识别，以及像素 Atlas 与 3D 角色联动。
- 不依赖 AI 也能完成的人工拆分，以及用于减少初始分类工作的可选 AI。
- 组件显隐、像素选择、Unknown、合并、拆分、重分类、关系编辑和草稿撤销/重做。
- 不覆盖旧结果的版本历史、差异比较和逐像素来源。
- 单组件 Part、完整大类 Bundle，以及冲突可见的修补与组合。

### 暂时不做

- 不把隐藏内容候选描述成原作者真实画过的像素，也不在发布门通过前默认开启 Completion。
- 不把产品扩展成自由绘画软件、通用 AI 皮肤生成器或在线素材交易市场。
- 不承诺未经验证的一键安装、云端托管、多人实时协作或自动资产发布。

### 完成后可以怎样演示

导入一张带长发、服装和饰品的皮肤，运行一次初始识别；在检查页单独显示长发，用框选或同色选择修正少量边界，把不确定像素退回 Unknown，再确认成新版本。随后在旧版与新版之间比较变化，下载修正后的皮肤，并把长发保存为一个可独立预览和再次应用的 Part。

## 为什么选择这个方向

- **主方向：** 本地、可回退的 Minecraft 皮肤语义拆分与组件复用工具。
- **选择原因：** “从整张皮肤得到可复用组件”容易理解，也能形成一次完整、有明显结果的体验；版本、来源和可选 AI 都围绕这件事提供安全与效率。
- **吸收要素：** 从极简工具方向吸收四步玩家流程；从资产工作室方向吸收历史、Part、Bundle、修补和组合，但把复杂管理能力留在高级工作室。
- **暂不进入：** 不以“自动生成新皮肤”或“在线素材平台”为产品入口，它们会稀释可靠拆分与复用的核心承诺。

## 当前状态

- **已经可用：** 皮肤导入与预览、可见像素语义拆分、人工检查修正、玩家四步界面、不可变版本与逐像素来源、可复用 Part、完整大类 Bundle、组件修补、多部件组合，以及可选的 AI 初始分类和候选排序。确定性浏览器测试已覆盖主要编辑与保存流程。
- **正在验证：** 隐藏内容候选（Completion）仍放在默认关闭的实验入口中。保守候选算法已通过离线安全门，但真实 AI 排序与完整发布证据尚未满足发布条件，因此它不属于默认玩家流程。
- **仍是设想：** 面向非开发者的一键安装包、正式托管服务、多人协作与在线资产交换，需要单独验证是否值得进入产品方向。

## 关键未知

### 初始拆分能否稳定节省时间

- **为什么重要：** 如果不同风格皮肤都要大量返工，智能识别就不能成为明确的效率收益。
- **最小验证：** 选取有许可的 Wide/Slim、透明外层、长发、裙装和复杂饰品样本，记录从导入到可用组件所需时间、Unknown 比例和人工修正量，并与纯手工复制对比。
- **可能影响：** 若收益只出现在少数风格，应收窄默认识别范围，并把高效人工工具放在更突出的位置。

### 第一次使用者能否独立完成四步流程

- **为什么重要：** 主方向要求普通创作者无需理解版本内部结构或 AI 配置，也能得到可保存结果。
- **最小验证：** 让未接触项目的用户完成导入、修正一个误分类区域并保存 Part，观察卡点和需要解释的术语。
- **可能影响：** 若仍需要技术指导，应继续减少默认页面信息，把历史与资产治理细节移入高级工作室。

### 拆出的组件是否真的会被再次使用

- **为什么重要：** 产品价值不只在“看见拆分结果”，还在于用户愿意保存并把组件带到后续皮肤中。
- **最小验证：** 让用户从一张皮肤保存 Part，再应用到另一张兼容皮肤，记录成功率、冲突处理次数和最终是否保留该结果。
- **可能影响：** 如果跨皮肤复用成本过高，应优先改进兼容提示、预览和修补体验，而不是扩大自动识别范围。
