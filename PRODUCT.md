# MCSkinSplit

> 面向 Minecraft 皮肤创作者与资源整理者的语义拆分工作台：把一张完整皮肤变成可理解、可修正、可复用的头发、服装、饰品等组件，同时保留每次修改的版本历史。

## 产品速览

- **面向谁：** 制作、整理或复用 Minecraft 皮肤的玩家、像素美术、资源包作者和小型创作团队。
- **使用时刻：** 用户手里有一张 64×64 皮肤，希望识别其中的语义部件、修正边界，再单独保存或组合使用。
- **当前问题：** 皮肤只是铺在固定 UV 上的一张平面图；同一件头发或衣服会跨越多个身体面，靠肉眼逐像素复制既费时，也容易破坏透明度、图层和 Slim/Wide 手臂布局。
- **核心体验：** 导入皮肤，智能识别可见组件，在 2D/3D 中检查并手工修正，最后选择明确版本并导出皮肤、单个组件或完整大类。
- **第一版：** 不依赖 AI 也能完成一张皮肤的精确语义拆分；启用 AI 时只减少初始分类工作，所有正式结果仍由用户确认并形成可回退的新版本。

## 它是什么

MCSkinSplit 不是把皮肤粗略切成头、身体和四肢图片，而是识别“长发主体”“上衣”“左手套”“鞋子”“头饰”这类真正可使用的语义组件。每个组件保存的是原皮肤中精确的像素范围，因此一个组件可以跨越多个身体部位和 Base/Outer 图层，同一 UV 面也可以包含多个不同组件。

产品默认呈现四步玩家流程：**导入皮肤 → 智能识别 → 检查修正 → 保存导出**。需要管理历史、部件库、修补工程或多皮肤混搭时，再进入高级工作室。这样既保留专业能力，也不要求第一次使用的玩家先理解 Branch、Instance ID、Provider、Schema 或 Composition。

## 为什么值得做

现有皮肤编辑通常围绕整张 Atlas 或单个身体面展开。用户想把某张皮肤的长发、衣服或配饰复用到另一张皮肤时，必须自己判断哪些像素属于同一物品、哪些在 Outer 层、Slim 与 Wide 手臂是否兼容，以及覆盖冲突该怎样处理。一次失误就可能改坏原文件，后续也很难解释某个像素来自哪里。

MCSkinSplit 只改变这个关键环节：把“整张皮肤”转换成带名称、分类、精确遮罩和来源记录的组件，并让每次确认都成为不可变的新版本。用户可以放心试错、回看和复用，而不是在一个 PNG 上反复覆盖。

## 用户怎样使用

1. 导入一张 64×64 PNG，确认系统识别的 Slim/Alex 或 Wide/Classic 手臂布局。
2. 启动可选的智能识别，或直接进入人工拆分；系统只分类源图中真实可见的像素。
3. 在组件树、2D 像素画布和 3D 预览之间检查结果，使用画笔、矩形、同色魔棒、整面选择、Unknown、合并、拆分、改分类和组件关系等操作修正。
4. 确认操作后创建一个新版本；原版本不会被原地修改，必要时可回退或从历史版本继续分支。
5. 选择要保存的结果，下载完整皮肤 PNG，或把所选组件保存为可复用 Part，把同一大类保存为 Bundle。

## 核心组成

### 1. 可理解的语义拆分

识别结果以头发、服装、皮肤、五官、饰品等语义组件组织，不把身体块或 UV 面冒充最终部件。组件始终绑定精确像素遮罩，边界可被用户逐像素检查和修改。

### 2. 玩家可控的检查与修正

普通流程提供组件优先的三栏检查界面：左侧选择和显隐组件，中间用 2D/3D 查看与圈选，右侧确认名称、分类和操作。草稿支持撤销、重做以及镜像或接缝扩选预览；只有明确确认才会生成正式版本。

### 3. 可回退的版本与来源

导入、智能识别、人工修正、部件应用、修补和混搭都不会覆盖旧结果。系统保存版本关系和逐像素来源，让用户知道哪些像素来自源图、人工创作或已接受的生成候选。

### 4. 可复用的部件与安全混搭

组件可以保存为带纹理、写入遮罩、来源和预览的 Part，也可以按头发、服装或配饰形成完整大类 Bundle。应用和混搭前会报告模型、图层与像素冲突，不会静默覆盖。

### 5. 明确受限的隐藏内容候选

被头发或饰品遮住的内容不可能从原 PNG 中被真实“还原”。实验性 Completion 只生成可审核候选，展示纹理、写入范围和生成来源；用户可以微调、接受或保留原结果。任何接受都记录为推测生成内容，当前默认玩家流程不会自动开启或发布它。

## 第一版产品

### 最小完整闭环

导入一张皮肤 → 获得可见组件初始分类 → 在 2D/3D 中修正组件边界与分类 → 选择已确认版本 → 导出完整 PNG、单个 Part 或完整大类 Bundle。

### 第一版包含

- 64×64 RGBA 皮肤导入、Slim/Wide 布局和 2D/3D 联动预览。
- 可选 AI 初始分类，以及不依赖 AI 的完整人工拆分能力。
- 组件显隐、像素选择、Unknown、合并、拆分、重分类、关系编辑和草稿撤销/重做。
- 不可变版本、回退、分支和精确来源记录。
- 单组件 Part、完整大类 Bundle、修补与冲突可见的混搭。

### 暂时不做

- 不把推测的隐藏像素宣称为原作者真实绘制内容。
- 在评测与真实浏览器门未全部通过前，不把 Completion 放进默认玩家流程。
- 不提供未经验证的云部署、多人实时协作、在线素材市场或自动发布资产。

### 完成后可以怎样演示

现场导入一张带长发、服装和配饰的皮肤，运行一次智能识别，在三栏检查页隐藏/单独查看组件，用魔棒或矩形修正少量像素，把错误区域退回 Unknown，再确认成一个新版本。随后切换旧版与新版观察真实 RGBA/归属/分类差异，最后下载皮肤，并把长发保存为可复用 Part。高级演示可再展示一次冲突预览和显式处理后的混搭结果。

## 为什么选择这个方向

- **主方向：** 玩家优先的语义拆分与可复用部件工作台。
- **选择原因：** 它能用四个动作说清楚，并形成从整张皮肤到可复用结果的完整体验；AI、版本和像素安全都服务于这条体验，而不是成为用户必须先学习的概念。
- **吸收要素：** 保留专业版的历史、资产治理、修补、混搭和受限补全，统一放入高级工作室或实验入口。
- **暂不进入：** 不把产品扩张成通用像素画工具、素材交易平台或自由生成皮肤模型；这些方向会稀释“可靠拆分与复用”的核心价值。

## 当前状态

- **已经可用：** 无损 PNG/UV 处理、可见语义拆分、高效人工修正、玩家四步界面、不可变版本、逐像素来源、Part 2.0、Bundle、组件修补、冲突可见的多部件混搭，以及可选的受限 AI 分类与候选排序。确定性浏览器回归已经覆盖语义编辑、Completion 接受/拒绝、候选微调、latent Part 发布、响应式布局、键盘与触摸操作。
- **正在验证：** 隐藏内容 Completion 保持 feature flag 默认关闭。保守候选算法 v2 已通过离线安全门，但还没有取代已发布的 v1 运行合同；真实 AI 排序与完整发布证据仍未满足 M21 发布门。
- **仍是设想：** 解除 Completion 默认关闭、面向非开发者的一键安装包、正式生产部署和多人协作都需要独立证据与后续产品决定。

## 关键未知

### 不同风格皮肤的首次识别质量

- **为什么重要：** 初始分类如果需要大量返工，智能识别就不能明显缩短用户任务。
- **最小验证：** 用有许可的 Wide/Slim、透明层、长发、裙装和复杂饰品样本记录组件边界错误、Unknown 比例与人工修正用时。
- **可能影响：** 若收益集中在少数风格，应收窄默认识别范围，把人工工具作为主入口，而不是增加更多不可控模型步骤。

### 第一次使用者能否独立完成四步流程

- **为什么重要：** 产品主方向依赖“普通玩家不必理解内部术语”这一假设。
- **最小验证：** 让未接触项目的用户完成导入、修正一个误分类像素并导出 Part，观察是否需要解释 Branch、ID、Mask 或 Provider。
- **可能影响：** 若仍需要技术指导，应进一步减少默认页面信息，把更多状态移入高级详情。

### 隐藏内容候选是否值得进入默认流程

- **为什么重要：** Completion 会生成原图不存在的推测像素，错误结果会直接损害用户信任。
- **最小验证：** 在隐藏真值合成集上继续评测像素精度、颜色误差和负例安全，并补齐真实 AI 排序与浏览器接受/拒绝证据。
- **可能影响：** 只有完整发布门通过才考虑解除 feature flag；否则长期保留为高级实验工具。
